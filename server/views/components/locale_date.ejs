<%
  var d = new Date(date);

  var month = __(`views.components.locale_date.months.${d.getMonth()}`);

  var day = d.getDate();

  var fullYear = d.getFullYear();
  
  var dateString = __('views.components.locale_date.template', { month, day, fullYear });
%>

<%- dateString %>